# Last update March 2024 (rlh)

springdocs:
  mdb:
    url: not-define-mongodb-url
  ds:
    username: username
    password: password
    url: not-defined

aws:
  region: not-defined
  access:
    key:
      id: not-defined
  secret:
    key: not-defined

# This is the permit where all content events are notified
content:
  greeting: "Welcome to content microservice from the config server, 'default configuration'."
